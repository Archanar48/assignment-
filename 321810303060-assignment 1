          #simple calculator for the operators
          
# + (addition)
x=20
y=30
print(x+y) 
#output:-50
# - (subtraction)
x=50
y=20
print(x-y)
#output:-30
# * (multiplication)
x=4
y=6
print(x*y)
#output:-24
# / (divition)
x=20
y=2
print(x/y)
#output:-10.0
# % (modulus)
x=25
y=2
print(x%y)
#output:-1
# ** (exponent)
x=3
y=3
print(x**y)
#output:-27
# // (floor divition)
x=23
y=4
print(x//y)
#output:-5

               #calculate simple interest
               
P = 1000
R = 1
T = 2
# simple interest
SI = (P * R * T) / 100
print("simple interest is", SI)
#output:-simple interest is 20.0

               #calculate area of circle
               
PI=3.14
r=6
area=PI*r*r
print(area)
#output:-113.039

               #calculate are of triangle
a=20
b=20
c=10
# calculate the semi-perimeter  
s = (a + b + c) / 2  
# calculate the area  
area = (s*(s-a)*(s-b)*(s-c)) ** 0.5  
print(area)
#output:-96.824

  #program to temperature in celsius to fahrenheit
  
celsius = float(input('Enter temperature in Celsius: '))  
# calculate temperature in Fahrenheit  
fahrenheit = (celsius * 1.8) + 32  
print('%0.1f  Celsius is equal to %0.1f degree Fahrenheit'%(celsius,fahrenheit))
#output:-Enter temperature in celsius:6
#6.0 celsous is equal to 42.8 degree  fahrenheit

                 #calculate area of rectangle
                 
width = float(input('Please Enter the Width of a Rectangle: '))
height = float(input('Please Enter the Height of a Rectangle: '))
# calculate the area
Area = width * height
print("\n Area of a Rectangle is: %.2f" %Area)
#output:-please enter the width of a rectangle:4
#please enter the height of a rectangle:8
#area of rectanglr is:32

                   #calculate perimeter of a square
                   
side = input("Enter side length of square: ");
slength = int(side);
perimeter = 4*slength;
print("\nPerimeter of Square =", perimeter);
#output:-enter side length of a square:4
#perimeter of square=16

                #calculate circumference of a circle
                
import math
radius = float(input("Enter the radius of the circle : "))
circumference = 2 * math.pi * radius
print("Circumference of the circle is : %.2f" % circumference)
#output:-enter the radius of the circle:6
#circumference of the circle is:37.7p

                #program to swap two numbers

x = 5
y = 10
# To take inputs from the user
#x = input('Enter value of x: ')
#y = input('Enter value of y: ')
# create a temporary variable and swap the values
temp = x
x = y
y = temp
print('The value of x after swapping: {}'.format(x))
print('The value of y after swapping: {}'.format(y))
#output:the value of x after swapping:10
#the value of y after swapping: 5